import math
long = float(input())  # Longueur

print(long * math.cos(0 * math.pi / 180), long * math.sin(0 * math.pi / 180))  # Affichage coordonnées (x,y) sommet angle 0
print(long * math.cos(60 * math.pi / 180), long * math.sin(60 * math.pi / 180))  # Affichage coordonnées (x,y) sommet angle 60
print(long * math.cos(120 * math.pi / 180), long * math.sin(120 * math.pi / 180))  # Affichage coordonnées (x,y) sommet angle 120
print(long * math.cos(180 * math.pi / 180), long * math.sin(180 * math.pi / 180))  # Affichage coordonnées (x,y) sommet angle 180
print(long * math.cos(240 * math.pi / 180), long * math.sin(240 * math.pi / 180))  # Affichage coordonnées (x,y) sommet angle 240
print(long * math.cos(300 * math.pi / 180), long * math.sin(300 * math.pi / 180))  # Affichage coordonnées (x,y) sommet angle 300